import React from 'react';
import ReactDOM from 'react-dom';
import Jhdees from './Components/Jhdees';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<App />, div);
});
